<?xml version="1.0" encoding="utf-8"?>
<!-- MindSprint Trivia native splash background: solid brand navy, with the
     logo mark centered on top, shown by Android before the Flutter engine
     draws its first frame. The logo is supplied as pre-sized bitmaps across
     the standard density buckets (drawable, drawable-hdpi, drawable-xhdpi,
     drawable-xxhdpi, drawable-xxxhdpi), the same approach already used for
     ic_launcher.png, so no version-gated sizing attributes are needed here. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/launch_logo" />
    </item>
</layer-list>
