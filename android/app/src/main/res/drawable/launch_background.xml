<?xml version="1.0" encoding="utf-8"?>
<!-- Fallback native splash background (pre-API 21; unreachable in this app
     since minSdk is 23). Kept as a solid brand color for consistency -- the
     logo version lives in drawable-v21, which is what real devices use. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background_color" />
</layer-list>
